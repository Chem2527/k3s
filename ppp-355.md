# PPP-355: Azure DevOps Verified Status & Required Change


---

## 1. CD Pipeline (`pipeline_CD.yml`) Status

###  Current Status: ALREADY CONFIGURED (No changes needed!)

 checked `Pipelines/pipeline_CD.yml` in both `fintech_admin_webapp` and `fintech_webapp` in Azure DevOps. 

**`renderEnv: true` is ALREADY enabled in both repositories:**

```yaml
extends:
  template: pipeline-cd/flow/containerapp-deploy.yml@pipelines
  parameters:
    renderEnv: true
    secretEnv:
      REACT_APP_APPINSIGHTS_CONNECTION_STRING: $(REACT_APP_APPINSIGHTS_CONNECTION_STRING)
      REACT_APP_ENC_KEY: $(REACT_APP_ENC_KEY)
    envConfig:
      qa:
        variableGroup: ZB-FintechAdminWebApp-QA  # (ZB-FintechWebApp-QA for webapp)
      prod:
        variableGroup: ZB-FintechAdminWebApp-PROD  # (ZB-FintechWebApp-PROD for webapp)
```

> **Conclusion for CD:** **No changes required in `pipeline_CD.yml`.** It is already configured to inject environment variables into Azure Container Apps.

---

## 2. Variable Groups Status

### ✅ Current Status: ALREADY EXISTS (No changes needed!)

We verified that all 4 required variable groups exist in **Pipelines > Library**:
* `ZB-FintechAdminWebApp-QA`
* `ZB-FintechAdminWebApp-PROD`
* `ZB-FintechWebApp-QA`
* `ZB-FintechWebApp-PROD`

Each group already contains:
* `REACT_APP_APP_BASE_URL`
* `REACT_APP_DEVELOPMENT`
* `REACT_APP_ENC_ALGO`
* `REACT_APP_ENC_KEY`
* `REACT_APP_APPINSIGHTS_CONNECTION_STRING`

*(Optional: You can add `REACT_APP_APP_ENV` with value `qa` or `prod` if you want structured logging tagged with the environment name).*

---

## 3. The ONLY Change Needed: CI Pipeline (`pipeline_CI.yml`)

### ⚠️ Action Required in `Pipelines/pipeline_CI.yml`:
In both `fintech_admin_webapp` and `fintech_webapp`, **remove the `arguments: >-` block and the `--build-arg` lines** so Docker does not bake values at build time.

### Change Diff:

```diff
  extends:
    template: pipeline-ci/flow/build-acr.yml@pipelines
    parameters:
      buildMode: docker
      imageName: "fintechadminwebapp" # or "webapp"
      dockerfilePath: "$(Build.SourcesDirectory)/Dockerfile"
-     arguments: >-
-       --build-arg REACT_APP_APP_BASE_URL=$(REACT_APP_APP_BASE_URL)
-       --build-arg REACT_APP_DEVELOPMENT=$(REACT_APP_DEVELOPMENT)
-       --build-arg REACT_APP_ENC_ALGO=$(REACT_APP_ENC_ALGO)
-       --build-arg REACT_APP_ENC_KEY=$(REACT_APP_ENC_KEY)
-       --build-arg REACT_APP_APPINSIGHTS_CONNECTION_STRING=$(REACT_APP_APPINSIGHTS_CONNECTION_STRING)
```

